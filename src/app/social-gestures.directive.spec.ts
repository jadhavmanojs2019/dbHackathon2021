import { SocialGestureDirective } from './social-gestures.directive';

describe('SocialGesturesDirective', () => {
  it('should create an instance', () => {
    const directive = new SocialGestureDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
